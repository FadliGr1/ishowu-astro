---
interface Props {
  text: string;
  icon: string;
  link: string;
  bg: string;
  download: string;
}

const {text, icon, link, bg, download} = Astro.props;
---

<a href={link} class={bg} {download}>
  <img src={icon} alt={text} class="me-1" />
  {text}
</a>

<style></style>
